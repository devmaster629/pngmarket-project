# Staging / production — copy to `.env.staging` on the server (optional).
# If missing, config.php uses built-in Hostinger staging defaults when the
# request host is *.pngmarket.online.
#
#   copy .env.staging.example .env.staging

DB_HOST=localhost
DB_USER=u946456524_userstage
DB_PASSWORD=replace_with_hostinger_password
DB_NAME=u946456524_dbstage
DB_TABLE_PREFIX=oc_

REL_WEB_URL=/
WEB_PATH=https://stage.pngmarket.online/
